let rec Rev S =
     S eq '' -> ''
     | (Rev (Stern S)) @Conc (Stem S)
within
     Pairs S1 S2 =
      not (Isstring S1 & Isstring S2) -> 'both args not strings'
      | (Rev S1) @Conc (Rev S2)
 in Print ( Pairs 'abc' 'def')
